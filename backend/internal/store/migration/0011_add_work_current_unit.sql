-- 0011 · 工作正在做哪个单元（M7 U7.3.1）
--
-- ★★ 有它才谈得上「这次写入在不在边界内」——边界写在单元的契约里，
-- 不知道是哪个单元就查不到契约，而那时权限卡片只能说「不知道」，
-- 用户得自己判断 AI 要动的东西该不该动。
--
-- ★ 允许为空：澄清、规划阶段都还没开始做任何单元。

ALTER TABLE works ADD COLUMN current_unit_id TEXT NOT NULL DEFAULT '';
