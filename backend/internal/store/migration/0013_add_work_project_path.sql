-- 0013 · 工作属于哪个项目（左栏项目树要用）
--
-- ★★ 不记的话，用户打开应用看到一个空荡荡的项目——而工作明明就在库里。
-- `design/PARITY.md` 开篇记的正是这一类：「数据有却不显示，等于界面说谎」。
--
-- ★ 存**路径**而不是项目 id：项目可以被移除再加回来，那时 id 变了而路径没变，
-- 按 id 关联的话那些工作会集体失去归属。
-- （`project_id` 那一列 0001 就有，但从来没人往里写过——留着不动，
--  免得改动一个可能有存量数据的列。）

ALTER TABLE works ADD COLUMN project_path TEXT NOT NULL DEFAULT '';
