# 里程碑体系

> **里程碑是「用户能体验的一件事」，不是「技术做完的一层」。**
>
> 2026-08-07 重建：原来按技术分层划分（M0「地基：ACP 协议层」之类），
> 使用者根本无法观测，等于没有里程碑。现在每个里程碑都是一组
> **他能自己动手试、自己判断成没成**的功能。

用户视角的验收标准在 [`../acceptance.md`](../acceptance.md)（V0–V15）——
**那份是目的，本目录是施工图。**

---

## 六步流程 ★

每个单元都要走完这六步，**一步都不能跳**：

```
① 里程碑        这个单元属于哪个用户功能（必须能指到 V几）
② 拆分单元      写全五段契约：goal / allowed / forbidden / 验收标准 / stop
③ 单元实现计划   动手前写下：改哪些文件、什么顺序、怎么验
④ 单元测试（代码）★ 先写会失败的测试，跑一次确认是红的，再写实现
⑤ 操作测试（UI）★ 把应用真的跑起来，以用户视角点一遍
⑥ 通知用户测试   ⑤ 全过之后才告诉用户「V几 可以验收了」
```

### 第 ④ 步：单元测试

铁律 1。详见 [`../../rules/testing-strategy.md`](../../rules/testing-strategy.md)。
**先红过**的测试才算数，提交信息里要写出它的名字。

### 第 ⑤ 步：操作测试 ★ 最容易被跳过的一步

**「测试全绿」不等于「功能能用」。** 单测证明代码逻辑对，
证明不了按钮点得动、文案看得懂、报错看得明白、流程走得通。

必做：

| 做什么 | 怎么做 |
|---|---|
| 把应用跑起来 | `make dev`（用完 `make dev-stop`），或装好的 `.app` |
| 照着用户会做的操作走一遍 | 对照 `acceptance.md` 里那条 V 的「你怎么试」 |
| 逐条核对现象 | 对照「应该看到什么」，**包括文案是否与设计稿一致** |
| 把失败路径也试掉 | 对照「常见的做错的样子」——那些是看着像做完了其实没有的情况 |
| 留证据 | 截图 / 页面文本 / 真实响应。不是「我认为应该可以」 |

**有一条对不上就不要进第 ⑥ 步。**

### 第 ⑥ 步：通知用户

一句话说清「V几 可以验收了」+ 怎么试 + 你自己验到了什么。
**不要把测试压力转嫁给用户**——他是来验收产品的，不是来当测试员的。

**通知完立刻做下一个单元，不要等他回复。** 这是用户明确要求的：
「只需通知，自主运行」。等回复会让整条流水线停在一个不需要停的地方——
他有意见会主动说，没说就是没问题。

---

## 里程碑清单

| 里程碑 | 用户拿到什么 | 覆盖验收点 |
|---|---|---|
| [M0 · 基础框架](M0-foundation.md) | 装上、打开、看到完整轮廓 | ✓ 完成 |
| [M1 · 装得上，能一直更新](M1-install-and-update.md) | 更新不丢工作 | 进行中 |
| [M2 · 它有了角色、技能和记性](M2-roles-skills-memory.md) | 看得到 Duet 有哪几个「员工」、会什么、记得什么 | **现在做** |
| [M3 · 把我的项目交给它](M3-project-onboarding.md) | 交一个文件夹给它，交之前知道它会放什么进去 | 待开始 |
| [M4 · 开一个工作](M4-start-a-work.md) | 开一条独立 worktree，主工作区一字不动 | 待开始 |
| M5–M12 | 需求 → 计划 → 契约 → 证据 → 决策 → 记忆 → 调教 → 报表 | 验收条件见 [`roadmap.md`](../roadmap.md) |

**顺序由引用关系决定**（2026-08-08 重排，见 `design/DEPENDENCIES.md`）：
每个里程碑是用户能**独立做成的一件事**，且它依赖的东西在它之前都做好了。

上一版按技术分层切，做出来每块单测都绿，但用户拿不到一件完整的事——
对话页（拓扑序的 L7）做完了，而它上面六层全是空的，结果是一个「聊天框」。

★ **M5 及以后暂时没有单独文件**：施工图快做到时才建，
远期写死的单元拆分到那天必然已经过期。验收条件已经定死在 `roadmap.md`，拆单元时不许放宽。

重排前的里程碑在 [`archive/`](archive/)，**只作历史查阅**——
它的单元编号指的是重排前的东西，`make check-milestones` 整体豁免了那个目录。

---

## 编号规则

```
M<里程碑>                 M1
S<里程碑>.<子计划>         S1.2
U<里程碑>.<子计划>.<单元>   U1.2.3
```

编号**只增不复用**。单元合并或废弃时保留编号并标注。
分支名直接用编号：`feat/acp-u1-2-3-updater-ui`。

> **2026-08-07 之前的编号已废弃**（含旧的 `U0.x` 那一批）。那套按技术分层编的号
> 在本次重建中被吸收进各功能里程碑。已完成的工作见各里程碑的「已经就绪的地基」。
>
> ★ **现在的 `U0.x` 是新 M0（基础框架）的单元，与旧的同名编号不是同一批东西。**
> 文档里对旧编号的引用一律带「已废弃」标注，按标注读即可。

---

## 每个单元必须写全五段

| 段 | 内容 | 省了会怎样 |
|---|---|---|
| `goal` | 一句话，做完之后**用户**能多做什么 | 做着做着跑偏成技术自嗨 |
| `allowed_changes` | 允许改的文件/目录，具体到路径 | 边界形同虚设 |
| `forbidden_changes` | 明确禁止碰的东西 | 「顺手改一下」 |
| `acceptance_criteria` | 逐条，每条能变成一句可执行断言 | 无法测试先行 |
| `stop_conditions` | 什么情况必须停下来上报 | AI 自行扩大范围 |

**写不出可断言的验收标准 = 这个单元还没想清楚。** 回去拆。

`make check-milestones` 会逐条校验这五段与断言是否齐备。

---

## 状态标记

| 标记 | 含义 |
|---|---|
| `○` | 未开始 |
| `◐` | 进行中（标注分支名） |
| `✓` | 六步全走完（**标注提交号**） |
| `⊘` | 已废弃（**必须写原因**） |

★ **`✓` 的含义是六步全走完，不是「代码写完了」。**
第 ⑤ 步操作测试没做的单元一律只能标 `◐`。

---

## 什么时候改这些文档

| 情况 | 动作 |
|---|---|
| 单元做完（六步） | 就地标 `✓` + 提交号 |
| 发现单元拆错了 | **加新单元，不改已验收的** |
| 发现整个里程碑的假设错了 | 停下来找人 |
| 验收标准写不出断言 | 说明没想清楚，回去拆细 |

**不要在这里写实现细节。** 单元只说「做什么、边界在哪、怎么算完」，
「怎么做」在 [`../../spec/`](../../spec/) 的专题文档里。
