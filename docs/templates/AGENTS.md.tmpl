# AGENTS.md · {{DIR}}

> 本目录的规则。**就近优先**：与根 [`AGENTS.md`](/AGENTS.md) 冲突时以本文件为准。
> 生成后必须填实所有 `TODO`，留着占位符视为未完成（`make check-docs` 会拦）。

## 负责什么

TODO：一到三句话说清这个目录的职责。

## 不负责什么

TODO：**这一节比上一节重要。** 明确写出哪些东西不该出现在这里、应该去哪个目录。
边界模糊是 AI 把代码放错地方的头号原因。

## 依赖方向

| | |
|---|---|
| 允许 import | TODO |
| 禁止 import | TODO |

TODO：如果有 lint 规则强制这条依赖方向，写出规则名；没有就说明为什么不需要。

## 检查命令

```bash
TODO   # 本域的测试命令
TODO   # 本域的 lint / typecheck 命令
```

## 改这里之前必读

- TODO：相关设计文档路径与理由

## 本域特有的坑

TODO：踩过的坑、容易犯的错、看起来对但其实错的写法。
**每次在这个目录里翻车一次，就往这里加一条。** 这一节的价值随时间增长。
