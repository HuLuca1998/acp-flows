# 设计稿对照表

> **动任何界面之前先查这张表，动完更新它。**
> `make check-design-parity` 会验证设计稿里的每个区块都在这里表过态。

---

## 为什么有这张表

2026-08-08 用户打开应用第一句话是「为什么菜单没有项目列表和对话记录」。

查下来：左栏三块占位，只接了 Runtime 那一块，另外两块的数据后端明明有
（`/v1/projects` 返回了项目，`/v1/works` 返回了工作）。

**根因不是没读设计稿，是读它的方式是 grep。** 要做工具调用卡片就
grep「工具调用」，找到那一段照着做——而 grep 只能找到**你已经知道要找的
东西**。不知道左栏该有项目列表，就不会去 grep「项目列表」。

这是检索方式的结构性盲区，换一个 AI 来做同样会漏。所以把「设计稿上有哪些
区块」从记忆里搬出来，变成一张机器能核对的表。

---

## 状态取值（封闭枚举）

| 状态 | 意思 | 要求 |
|---|---|---|
| `一致` | 照设计稿做了 | — |
| `增强` | 比设计稿多做了点（不减） | 说清多了什么 |
| `简化` | 少做了一部分，**有理由** | 理由必填，且要说清用户损失了什么 |
| `待实现` | 还没做，欠着 | **必须指向具体单元**（如 `U4.2.2`） |
| `偏离` | 与设计稿不一致 | **必须写明谁批准的**——没批准就不许用这个状态 |

> **`简化` 与 `偏离` 的区别**：少做不算偏离，做得**不一样**才算。
> 拿不准就填 `偏离`——它逼你去要一个批准，而 `简化` 只要你写句话。

### 什么理由算充分

- ✅ 「设计稿这块依赖 M4 的证据模型，现在还没有数据可显示」
- ✅ 「设计稿是宽屏示意，窄栏下三列放不下，改成两列 + 折叠」
- ❌ 「先这样，以后再说」——那是 `待实现`，去填单元号
- ❌ 「这样实现简单」——实现难度不是用户体验的理由

---

## ★ 怎么把设计稿渲染出来看

```bash
cp -r design/_ds design/support.js "design/ACP Duet 1a.dc.html" frontend/public/design/
# 浏览器打开 http://localhost:5173/design/ACP%20Duet%201a.dc.html
```

**动对话页之前先这么看一眼。** 光读 HTML 源码看不出布局与信息密度——
2026-08-08 就是这么漏掉了整个对话页的九成内容。

（`frontend/public/design/` 已进 `.gitignore`，它是本地对照用的副本。）

---

## ★ 右栏原本那两块去哪了（2026-08-09）

`U4.2.2` 把右栏换成了设计稿 §四 的**「工作区」**。原来那里是
「时间线过滤器」+「引用」两块占位——**它们在设计稿里不在右栏**：

| 原来在右栏的 | 设计稿里它在哪 | 归属 |
|---|---|---|
| 时间线事件过滤器 | **计划面板那一侧**（右上角那个可拖动的面板） | `M6` |
| 引用文件 / 附件 | **输入框那一行**（`📎 附件 · ⌗ 引用 · 11/13`） | `M10` |

★ 词条一并删了（`context.timelineFilter` 等五条）。
留着的话，`check-i18n` 会一直报「有词条没人用」，而下一个人不知道
它们是欠着的还是废弃的——**记在这里比留一堆孤儿词条清楚**。

## ★ 对话页：设计稿 vs 实现（2026-08-08 逐项核对，2026-08-09 更新落位）

★★ **对话页不是一个里程碑能做完的。** 它是设计稿里信息最密的一屏——
上面每一块都挂着它自己的那条链路（角色 / 计划 / 契约 / 决策 / worktree）。
把它们塞进一个「做对话页」的单元，等于把六条链路的前置全部提前，
那正是 2026-08-08 之前的做法：对话页做完了，而它上面六层全是空的。

所以下表按**每一块依赖谁**来分配，不按「它们长在同一屏上」分配。

| 设计稿有 | 实现 | 归属 | 为什么是这个里程碑 |
|---|---|---|---|
| 对话按**角色**分（需求分析师 / 计划架构师 / 实现工程师），各带头像与标签 | ✓ 头像方块（`CL` / `CX`）+ 角色标签 + 左侧色条；**连续同一个角色只在第一条显示**头像与标签 | `U5.3.1` `U5.3.2` | 已完成。★ 2026-08-10 对照设计稿补的：只有标签的话一屏扫过去要逐条读文字；十条连续工具调用每条重复三个标签的话，用户要找的内容被自己的重复挤到了右边 |
| 需求分析师的 `requirement v2` `已冻结` 标签 | ✓ 消息头上的等宽小标签，版本与冻结态都来自事件载荷 | `U5.2.1` `U5.3.1` | 已完成 |
| 需求分析师那条消息下的**三个选项按钮**（不回滚，仅停止 / 回退到检查点 / 由用户逐次决定） | ✗ | **`M8`** | 那是 D2 决策的选项，要先有决策模型 |
| **冻结这一版**（设计稿没有画这个动作） | ⚠ **增强**：需求快照条上给了一个按钮 | `U5.2.1` | 设计稿画了「已冻结」这个**结果**，没画产生它的**动作**——而冻结由用户点、不由 AI 判断，他总得有个地方点。放在状态条下方，形态与「停下」一致 |
| 面包屑 `acp-engine › 取消运行中的 Agent turn › wt/work-08 · executing` | ⚠ **简化**：`项目 / 页面 / 工作标题 · 状态`，**没有 worktree 路径** | `M4` | 2026-08-10 补了第三段（标题 + 状态原值）。**用户损失**：看不出 worktree 在哪——那一段归右栏「工作区」，重复放两处会让顶栏挤到放不下标题 |
| 底部状态栏（`wt/work-08 · duet/work-06 → main@7c1de98 · 3 处改动 · 未提交`） | ✗ | **`M4`** | 全部是 worktree 信息，`U4.2.2` 的右栏拿到什么它就显示什么 |
| 右上角小卡：`计划 v5 · executing · unit-012` | ✗ | **`M6`** | 要先有计划版本号 |
| 左栏工作带**状态与进度**（`executing · 3/7`） | ⚠ 有状态无进度 | **`M6`** | `3/7` 是子计划里的单元完成数 |
| 计划架构师的 `plan v5` `3 子计划` `7 单元` + 两个按钮 | ✗ | **`M6`** | 同上 |
| 代码改动逐行带增删数（`edit crates/…/cancel.rs +64 −12`） | ✗ | **`M7`** | 实现工程师按契约干活时才有 diff |
| 注入清单（`skill:rust-test-first@2.1 · mem-203 · mem-188`） | ✓ 干活轮的 `injection` 事件行，记忆与 Skill 都列 | `U10.3.1` | 已完成。清单**由应用记**，不问 AI——AI 胡说「参考了 mem-99」也不影响它 |
| 折叠的「本轮小结」四行（计划 v4→v5 / 契约冻结 / 注入清单 / 处理方式） | ✓ 每轮结束的折叠块，四行来自计划、契约、注入、处置 | `U7.4.1` | 已完成（2026-08-10 真机核对第一轮验过） |
| 输入框的「询问写入」开关 | ✗ | **`M7`** | 它切的是权限裁决 |
| 决策卡片：三个选项 + 推荐（`不回滚，仅停止` / `回退到检查点` / `由用户逐次决定`） | ✗ | **`M9`** | D1/D2/D3 整块 |
| 记忆候选行（`记忆候选：… · 审核`） | ✓ 时间线上的候选卡片：正文全文 + 「收下 / 不要」都不预选中 | `U10.5.1` | 已完成。★ 还欠一次真机观察：真实开发单元里 AI 自己提候选（`U10.6.1` R5） |
| 输入框：附件 · 引用 3 个 · `11/13` | ⚠ 引用已做（chips + 真注入）；附件与 `11/13` 计数欠 | `U10.7.3` | 见下方对照表「引用」行 |
| 用户消息的右对齐气泡 | ✓ 右对齐、更亮的底、不占满整行、无头像无标签 | `U5.3.1` | 已完成。★ 前提是先有 `user_message` 事件——在此之前用户自己说的话根本不在时间线上 |
| 权限卡片（`请求写入 … 写入边界外` + 两个按钮） | ✓ | — | 重排前做的 |
| 时间线（消息 / 工具调用 / 本轮结束） | ✓ | — | 同上 |

**2026-08-08 时十七项里做了两项**，用户的原话是「只做了设计稿的 1%」——量级上他没说错。
2026-08-09 起 `M5` 补上了四项（角色标签 / 需求版本标签 / 用户消息气泡 / 冻结动作），十九项里做了五项。
2026-08-10 `U7.4.1` `U10.3.1` `U10.5.1` 又补了三项（本轮小结 / 注入清单 / 记忆候选卡片），十九项里做了八项。

★ 表里原本写的是**重排前的旧编号**（`U5.4.1` / `U3.3.1` 之类），
2026-08-09 更正成重排后的里程碑。旧编号指的是另一套东西，查它们要去
[`docs/plan/milestones/archive/`](../docs/plan/milestones/archive/)。


---

## ★ 覆盖率不是「区块表过态」就够了

`check-design-parity` 只保证**每个区块都表过态**。它挡不住这种情况：
一个区块标成 `一致`，实际只做了里面的十分之一。

2026-08-08 用户的原话：「特别是对话页面你只做了设计稿的 1%」。
查下来对话主区设计稿有 **91 段可见内容**，实现只有输入框 + 一条时间线。

所以**已实现的区块也要写清「里面有什么」**——见下表「实现 / 理由」列。
一个区块下面有五样东西而只做了一样，状态就该是 `简化`（并写清少了什么），
不是 `一致`。

---

## 对照表

| 设计稿区块 | 在哪 | 状态 | 实现 / 理由 |
|---|---|---|---|
| `项目` | 左栏 | **简化** | ✓ 项目树接真实数据：项目下挂工作，每条带状态词（2026-08-10 真机走查）。<br>**简化了两处**：① 工作行没有进度 `3/7`——单元完成数来自子计划，归 `M6` ② `waiting_user` 的蓝点未做，归 `M9`。<br>**用户损失**：看不出每条工作走到哪、哪条在等他决策 |
| `最近` | 左栏 | **待实现** `U5.1.1` | ✗ 骨架占位，`/v1/works` 已有数据。同上 |
| `工作` | 左栏 | **简化** | ✓ 项目下挂的工作列表、状态词、点击切换都有；进度 `3/7` 与 `waiting_user` 蓝点欠着（同上一行，归 `M6` / `M9`） |
| `进行中的工作` | 左栏 | **待实现** `U5.1.1` | ✗ 跨项目的「最近打开过」 |
| `Runtime` | 左栏底部 | **一致** | ✓ 圆点 + 名称版本 + 右对齐状态词，接 `/v1/runtimes` |
| `项目管理` | 设置页 | **一致** | ✓ 增删项目 |
| `环境检测` | 设置页 | **一致** | ✓ 六项检测 |
| `ACP Runtime 检测` | 设置页 | **一致** | ✓ claude / codex 的安装与登录态，含补救命令 |
| `Runtime 更新` | 设置页 | **待实现** `U5.2.1` | ✗ 检查 Runtime 自身的版本 |
| `应用更新` | 设置页 | **一致** | ✓ 检查 + 一键更新（M1 V2） |
| `GitHub 账号` | 设置页 | **待实现** `U4.3.1` | ✗ 改走系统 `gh`，见 open-questions Q41 |
| `通用` | 设置页 | **一致** | ✓ 语言、主题 |
| `按项目` / `按仓库绑定` | 设置页 | **待实现** `U4.3.1` | ✗ GitHub 账号的两种绑定方式 |
| `本地仓库` / `默认基线分支` | 设置页 | **待实现** `U5.1.1` | ✗ 项目详情里的 git 配置 |
| `ACP 事件 · 来自 Runtime` | 计划面板侧过滤器 | **待实现** `U6.3.1` | ✗ 组件写了但**没接线**：`TimelineFilter.tsx` 在 `frontend/src` 零引用，界面上没有任何入口——没接线的代码等于没写。设计稿里过滤器在计划面板那一侧（见上方 2026-08-09 注记），随计划面板一起做 |
| `应用事件 · 来自控制层` | 计划面板侧过滤器 | **待实现** `U6.3.1` | ✗ 同上 |
| `引用` | 输入框那一行 | **简化** `U10.7.3` | ✓ 「⌗ 引用」+ 可移除 chips（RefChip 原语），引用的文件**真的注入下一轮 prompt**（后端 `refs`，读不到整句拒绝、路径逃逸一律拒；组件与后端测试全绿，真机操作走查记在 `U10.6.1`）。<br>**简化**：① 附件未做 ② `11/13` 上下文计数未做——分母怎么算没有裁定（该记 open-questions）③ 文件靠手输相对路径，没有选择器 |
| `未提交改动` | 右栏工作区 | **一致** | ✓ 逐文件带增删行数（`+64 −12`）与合计，空态明说「工作区是干净的」（`U4.2.2`，2026-08-10 真机走查） |
| `仓库目录` | 右栏上下文 | **待实现** `U5.3.1` | ✗ 文件树 |
| `本次工作的 commit` | 右栏工作区 | **一致** | ✓ sha · 时间 · 「仅本地」标记 · 标题，空态明说；底部带「push 属于 D3」的说明原文（`U4.2.2`） |
| `附加到本轮上下文` | 右栏上下文 | **待实现** `U5.3.1` | ✗ 手动把文件加进上下文 |
| `需求` | 对话页顶部 | **简化** | ✓ 需求快照条已有：`requirement vN` · `N 条需求` · 「冻结这一版」（`U5.2.1`）。<br>**简化**：设计稿那条完整统计（`已映射 · 单元 3/7 · 已验收 · 待决策 · 重试`）没做——五个数字分别来自 `M6`（单元）`M8`（验收）`M9`（决策）的数据。<br>**用户损失**：看不出需求映射到哪些单元、验收走到哪 |
| `子计划 DAG` | 对话页 | **待实现** `U4.2.2` | ✗ 设计稿：`subplan-01 · ACP Runtime 抽象层 / accepted · 3/3`，<br>下挂 `unit-012 契约 v3 · attempt 2 / 查看契约 ›`、`unit-013 依赖 unit-012 · 契约未冻结` |
| `单元` | 计划面板 | **待实现** `U4.2.2` | ✗ 单元列表与状态 |
| `验收标准` | 计划面板 | **待实现** `U4.2.2` | ✗ 契约里的验收标准 |
| `变更历史` | 计划面板 | **待实现** `U4.2.2` | ✗ 计划改过几版、每次为什么<br>★ 后端已就绪（`U4.2.1` 的 `PlanVersion`），只差界面 |
| `检查点` | 计划面板 | **待实现** `U4.2.2` | ✗ 检查点列表<br>★ 后端已就绪（`U4.1.2`），只差界面 |
| `需求 → 证据` | 计划面板 | **待实现** `U4.3.2` | ✗ 需求到证据的追溯 |
| `待决策` / `待处理决策` / `等待你决策` | 主区 | **待实现** `U3.3.1` | ✗ D1–D3 决策卡片。<br>权限请求（`U3.1.3`）是其中一类，已做 |
| `work-08` | 窗口栏 | **简化** | ⚠ 面包屑现在是 `项目名 / 当前位置`，设计稿是 `项目 / 工作 / 单元`。<br>**理由**：还没有「当前工作」的概念（工作切换是 `U5.1.1`）。<br>**用户损失**：开着多个工作时看不出当前在哪个 |
| `重试` | 主区 | **待实现** `U3.3.1` | ✗ 失败后的重试入口 |
| `审查驳回原因` | 主区 | **待实现** `U4.3.2` | ✗ 独立审查的驳回理由 |
| `L2 项目记忆 · L3 跨项目记忆` | 记忆页 | **简化** `U10.7.1` | ★★ **用户裁定（2026-08-10）：「记忆页与设计图纸完全偏离」**——之前这行的「简化了两处」低报了差距。设计稿这一页的**主体是两栏**：左列条目卡（标题 + 正文预览 + kind/status 芯片），右侧详情栏（正文渲染 + frontmatter + 「系统数据库记录 · SQLite」字段表 + 变更历史 + 四个操作按钮），顶部还有范围选择器与真实路径。实现只有单栏极简列表——**页面主体缺失**。<br>**已有**（2026-08-10 真机走查过）：两栏与详情栏 ✓（`U10.7.1`：正文全文 + 「系统数据库记录」字段表 + 候选详情里「收下/不要」）；页头范围选择器 ✓（全部/跨项目/各项目，scope 真下到接口）；页框照设计稿撑满、列内滚动 ✓；四档筛选 + 候选审核（`U2.3.1`，Q25 两条裁定仍有效）。<br>**仍欠**：列表卡主行还是 id 不是正文标题（标题进索引才做得到，见 M10 S10.7 注记）；操作按钮（编辑/重新校验/提升/标失效）与变更历史 → `M11`；底部「新增记忆 / 导出 YAML」→ `M11` |
| `L4 Skill 库 · 版本化发布与回滚` | Skill 页 | **简化** `U10.7.2` | ★★ **用户裁定（2026-08-10）：「Skill 页与设计图纸完全偏离」**。设计稿是**两栏**：左列表（版本号 · 状态 · 命中与进度条），右侧 SKILL.md 详情栏（frontmatter 块 + 正文分节渲染 + 「在编辑器打开 ↗」/「查看源码」），顶部项目选择器与真实路径，底部「＋ 新建 Skill」「在 Finder 中显示」。实现只有左边那栏的简化版——**页面主体（详情栏）缺失**。<br>**已有**（2026-08-10 真机走查过）：两栏与详情栏 ✓（`U10.7.2`：`GET /v1/skills/{dir}/body`，frontmatter 块 + 正文渲染 + 「查看源码」）；页头项目选择器 ✓（`U3.1.2` 提前做了：`scope=project&project=<路径>` 真按项目扫，软链一律不跟）；页框照设计稿撑满、列内滚动 ✓；全局库列表 + 命中计数（`U2.4.1` `U10.4.1`）。<br>**仍欠**：条目上的命中进度条；「在编辑器打开 ↗」与底部两个按钮 → `U11.1.1` |
| `发现已有 Skill 目录` | Skill 页 | **待实现** `U3.1.2` | ✗ 扫描已有 Skill 是创建项目弹层的一部分，那时才有项目目录可扫。全局库的扫描与校验（同一套解析器）已在 `U2.2.1` 做好 |
| `角色先于 Runtime` | 角色页 | **简化** `U2.4.1` | ✓ 八行角色表已做（2026-08-08 真机验过）：角色/承担的操作 · 性格与语气 · Runtime · 会话模式 · 权限裁决 · 提示词，六列齐全；设计稿底部那两块说明（「这些设置对应 ACP 里的什么」「ACP 不提供、因此这里不设」）也照原文做了。<br>**简化了三处**，都指向 `U2.4.1` 之后的单元：① Runtime / 会话模式 / 权限裁决三个下拉做成**禁用态**（可编辑归 `U11.1.1`）② 「提示词 ✎」是禁用按钮（同上）③ 底部「＋ 自定义角色 / 恢复推荐绑定 / ⟳ 初始化角色集」三个按钮**未做**——八个角色内置即用，「初始化」无对应动作。<br>⚠ **两处刻意偏离**：<br>(a) 设计稿把 codex 绑到 `auto`——那是 codex **0.16.0 的旧档名**，1.1.7 已改成 `read-only` / `agent` / `agent-full-access`。**批准**：`acp-field-notes.md` §7 裁定 2「笔记为准」+ 2026-08-08 用户确认。**用户损失**：无——照设计稿做会直接报错。<br>(b) 说明文案里设计稿写 `session/set_mode`，界面上改成 `session/set_config_option`（旧的只作降级）。**批准**：同上裁定 3「官方已挂废弃告示」。**用户损失**：无——照抄的话用户按那句话去查日志会找不到那条帧。<br>★ 设计稿的「隔离」三开关与「D3 外部动作」两块**未做**，归 `U11.1.1` 与 `U9.1.1` |
| `Runtime 使用` | 报表页 | **待实现** `U5.5.1` | ✗ 整页是占位 |
| `单元一次通过率` | 报表页 | **待实现** `U5.5.1` | ✗ 同上 |
| `平均单元耗时` | 报表页 | **待实现** `U5.5.1` | ✗ 同上 |
| `每日已验收单元` | 报表页 | **待实现** `U5.5.1` | ✗ 同上 |
| `近 30 天 · 全部项目` | 报表页 | **待实现** `U5.5.1` | ✗ 同上 |
| `系统数据库记录 · SQLite` | 设置页 | **待实现** `U5.2.1` | ✗ 数据库位置与大小 |

---

## 怎么用

**动界面之前**：在表里找到对应区块，看它现在是什么状态。

**动完之后**：更新那一行。新增了设计稿里没有的东西 → 加一行，状态填 `增强`。

**发现设计稿有而表里没有** → `check-design-parity` 会红，把它加进来。

**要偏离设计稿** → 状态填 `偏离`，写明谁批准的。**没批准就别改**——
按设计稿做，把疑问记在 `docs/plan/open-questions.md` 里等裁决。
