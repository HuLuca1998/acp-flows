# 设计稿完整清单

> 2026-08-08 逐屏渲染 + DOM 遍历拆出来的，不是 grep 的结果。
> 用来重写里程碑与验收条件；每一项都要能回答「归哪个里程碑、验收标准是什么」。
>
> 渲染方式见 [`PARITY.md`](PARITY.md) 开头。

---

## 整体

**一屏三栏 + 三个弹层**，1634×1195。

```
┌─ 顶部标注条（设计说明，不是界面的一部分）
└─ 应用主体 1600×958
   ├─ 窗口栏 42px：交通灯 · 折叠开关 · 面包屑 · 新建/搜索
   ├─ 左栏 252px（可拖宽）
   ├─ 主区 1038px
   └─ 右栏 300px（可拖宽）
   
   弹层（默认隐藏）：创建项目 · 新建工作 · D2 决策详情
```

---

## 一 · 窗口栏

| 元素 | 说明 | 我的状态 |
|---|---|---|
| 交通灯占位 | macOS 三个圆点 | ✓ |
| ⧉ 折叠左栏 | | ✓ |
| ⊞ 计划面板 / ⊟ 右栏 | 两个折叠开关 | ✓ |
| ✎ 新建工作 | 打开「新建工作」弹层 | ✗ |
| 🔍 搜索 ⌘K | | ✗ |
| 面包屑 | `对话 · acp-engine / 取消运行中的 Agent turn / rust-test-first ⋯ wt/work-08 · executing` | ⚠ 只有 `项目 / 页面` |
| 返回按钮 | `← 上一处` | ✗ |

---

## 二 · 左栏 252px

| 区块 | 内容 | 我的状态 |
|---|---|---|
| 导航 | 报表 · 记忆 · Skill · 角色与 Runtime · 设置 | ✓ |
| **项目** | 标题 + 「＋ 创建项目」 | ✓ 按钮已能点 |
| ↳ 项目条目 | `▾ acp-engine`，可折叠 | ✓ |
| ↳ 工作条目 | `取消运行中的 Agent turn` + `executing · 3/7` + **蓝点**（有待处理） | ⚠ 有状态，**无进度、无提醒点** |
| ↳ 工作条目 2 | `ACP 事件流补齐推送通道` + `waiting_user · D2` | ⚠ 同上 |
| ↳ `＋ 新建对话` | 每个项目下面一个 | ✓ |
| **最近** | 跨项目的最近打开（`会话池复用与超时策略` 等三条） | ✗ 骨架 |
| **RUNTIME** | `● Claude 0.63.0 就绪` / `● Codex 1.1.7 运行中` | ✓ |

---

## 三 · 主区 1038px（对话）

### 3.1 右上角浮标

`计划 v5 · executing` / `unit-012 · 取消幂等性与现场保留` —— ✗ 没做

### 3.2 消息流：**每条都带角色**

| 角色 | 标签 | 内容形态 | 我的状态 |
|---|---|---|---|
| 用户 | 右对齐气泡 | 纯文本 | ⚠ 无气泡样式 |
| `Claude · 需求分析师` | `requirement v2` `已冻结` | 文字 + **三个选项按钮**（不回滚，仅停止 / 回退到检查点 / 由用户逐次决定） | ✗ 无角色无选项 |
| `Claude · 计划架构师` | `plan v5` `3 子计划` `7 单元` | 文字 + 两个按钮（查看计划 ⊞ / unit-012 契约 v3） | ✗ |
| `Codex · 实现工程师` | `unit-012` `attempt 2` `契约 v3` `执行中 2:14` | **代码改动列表**（`edit crates/…/cancel.rs +64 −12`）+ `bash cargo test` + 运行中转圈 | ✗ |
| ↳ 注入行 | `注入 skill:rust-test-first@2.1 · mem-203 · mem-188` | | ✗ |
| ↳ 思考摘要 | `正在权衡：幂等标记放 session 还是 turn 层——一once session，取消后接轮次也应无效` | | ✗ |
| ↳ 权限卡片 | `Codex 请求写入 crates/…/events.rs 写入边界外 [允许一次][拒绝]` | | ✓ **做了** |
| ↳ 本轮小结（四行） | 计划 `v4 → v5` 新增 unit-013「查看差异」<br>契约 `unit-012 v3` 已冻结 · 写入边界 2 项 / 禁止 2 项「查看契约」<br>注入 `mem-203 · mem-188 · skill:rust-test-first@2.1`「注入清单」<br>已入队 · 单元结束后评估影响「处理方式」 | ✗ |

### 3.3 输入区

| 元素 | 我的状态 |
|---|---|
| 多行输入 `补充说明、纠正范围，或直接提问…` + `Shift+Enter 换行` | ⚠ 单行裸输入框 |
| 📎 附件 · ⌗ 引用 · `11/13` · `询问写入` 开关 | ✗ |
| 发送按钮（圆形 ↑，`发送 · Enter`） | ⚠ 文字按钮「开始」 |
| 引用条 `引用 · 3`：`acp-protocol.md ✕` / `~/notes/acp 只读 · 8 文件 ✕` / `截图 2026-08-06.png ✕` | ✗ |

### 3.4 底部状态栏

`● acp-engine · ⑂ wt/work-08 · ⑂ duet/work-06 → main@7c1de98 · 3 处改动 · 未提交` +
右侧 `Claude 就绪 · Codex 运行中` —— ✗ 没做

---

## 四 · 右栏 300px「工作区」

| 区块 | 内容 | 我的状态 |
|---|---|---|
| 头 | `wt/work-08` / `← main@7c1de90 · 领先 3 commit` | ✗ |
| **未提交改动 · 3** | 三个文件各带 `+64 −12` / `+118 新增` / `+2`，合计 `+184 −12`，`均在写入边界内`，「查看 diff」 | ✗ |
| **本次工作的 commit · 3** | `a1c9f30 2 分钟前 · 验收后自动提交 / unit-011: 抽出 Runtime 能力探针` 等三条，标「仅本地」 | ✗ |
| 说明 | 「单元验收通过后自动提交到工作分支；push 与发 PR 属于 D3，需你逐次授权」 | ✗ |
| 两个按钮 | 「在编辑器打开」「请求 push 授权」 | ✗ |

> ★ 我现在的右栏是「时间线显示（过滤器）」+「引用」两块占位——
> **与设计稿的右栏完全不是一回事**。过滤器在设计稿里属于计划面板那一侧。

---

## 五 · 弹层 1：创建项目

`添加本地仓库 → 初始化 .acpflows → 导入已有 skills`

| 元素 | 内容 |
|---|---|
| 仓库目录 | `~/work/acp-engine` + 「选择文件夹…」 |
| 将创建 | `＋ acp-engine/.acpflows/`（skills/ · memory/ · project.yaml） |
| 将追加 | `＋ .gitignore` — 忽略 `.acpflows/runs/`（运行记录不入库） |
| GitHub 账号 | `remote git@github.com:wyy-work/acp-engine.git` → `wyy-work`，「管理账号 ↗」 |
| **发现已有 Skill 目录 · 2** | `.claude/skills/` 3 个 SKILL.md<br>`tools/agent/skills/` 1 个 SKILL.md · **缺 description**<br>`.acpflows/skills/`<br>扫描 `**/skills`（跳过 node_modules 与 target） |
| 提示 | 「创建项目不会开始任何工作；之后在项目下『＋ 新建对话』才会切 worktree」 |
| 按钮 | 取消 / 创建项目 |

**我的状态**：✗ 完全没有。现在「创建项目」只是跳到设置页的一个列表。

---

## 六 · 弹层 2：新建工作

`选择本机 Git 仓库 → 基线分支 → 主 worktree`

| 元素 | 内容 |
|---|---|
| 本地仓库 | `~/work/acp-engine` · `已检测到 .git · 无未提交改动` + 「选择文件夹…」 |
| 默认基线分支 | `main` / `develop` / 「指定 commit…」三选一 |
| 说明 | 「以后每项工作都从这个分支切出独立 worktree；原工作区不受影响」 |
| 将创建 | 分支 `duet/work-09`、路径 `~/.duet/worktrees/acp-engine/work-09`、基线 `main @ 7c1de90` |
| 失败说明 | 「创建失败时工作进入 `initializing_failed`，不会退回原目录执行」 |
| 按钮 | 取消 / **创建 worktree 并开始** |

**我的状态**：✗ 完全没有。现在是主区一个输入框直接建。

---

## 七 · 弹层 3：D2 决策详情

`D2 · 高等级决策 · 扩展 EngineEvent 公开枚举`

| 区块 | 内容 |
|---|---|
| 版本切换 | `v3 当前` / `v2` / `v1` · 冻结于 `ck-07` · 由单元设计师产出 |
| **契约全文**（YAML） | `unit_id` `subplan_id` `contract_version: 3` `based_on_plan_version: 5`<br>`goal:` `allowed_changes:` `forbidden_changes:` `test_strategy:` `stop_conditions:` `inject:`<br>`← attempt 1 命中`（标出上次卡在哪） |
| **验收标准 + 证据** | `✓ ev-441` / `✓ ev-440` / `○ 无证据` + 「查看证据 ⤢」 |
| 「修订契约版本」 | |
| **证据四类** | Git diff · 测试输出 · 命令记录 · 审查意见 —— 「由应用直接采集，非 Agent 转述」 |
| Git diff | `ev-440 +64 −12` / `ev-441 +118` / `ev-438 已废弃 · superseded`<br>带真实 diff 片段（`@@ -18,6 +18,32 @@` 那种） |
| 审查意见 | `Claude · 实现审查员` |
| **三个选项 + 推荐** | A `EngineEvent 加 Cancelled`（**推荐**）：向后兼容；订阅端需加分支。影响 3 个文件、1 条公开契约，需新增 contract v4 与一条迁移说明<br>B `Stopped{reason}`：不动公开契约，语义变模糊；后续恢复逻辑可能需要再拆<br>C `回退到 ck-07 并重新规划`：丢弃当前 attempt，创建 plan v6；已验收的 subplan-01 保持有效 |
| 说明 | 「决定会写入 Decision 记录并生成新的契约版本；D3（push / 发布 / 删除）仍需逐次授权」 |
| 按钮 | 稍后决定 / **确认并继续** |

**我的状态**：✗ 完全没有。

---

## 八 · 角色与 Runtime 页

**「角色先于 Runtime」** —— 一张「角色与 Runtime 绑定」表。

| 列 | 说明 |
|---|---|
| 角色 / 承担的 AI 操作 | 如「需求分析师 / `clarify · snapshot`」 |
| 性格与提示语气 | 如「追问式，逐条确认，不放过『大概/应该』」 |
| Runtime | `claude ⌄` 下拉 |
| 会话模式 `set_mode` | `plan ⌄` 下拉 |
| 权限裁决 | `逐条询问 ⌄` 下拉 |
| 提示词 | 「提示词 ✎」可编辑 |

设计稿里列出的角色（每行一个）：

| 角色 | 承担的操作 | 性格 |
|---|---|---|
| 需求分析师 | `clarify · snapshot` | 追问式，逐条确认，不放过「大概/应该」 |
| 计划架构师 | `plan · subplan_dag` | 结构优先、克制，不写含糊单元 |
| 单元设计师 | `unit_contract` | 把交接写成契约而不是 Prompt，边界写死 |
| 实现工程师 | （对话里出现：`Codex · 实现工程师`） | — |
| **实现审查员** | 独立会话审查 diff 与证据 | 怀疑式，「全绿」不等于验证过；无证据即未通过 |

**角色详情卡**（设计稿里展开的那个）：

```
Claude · 实现审查员          claude · sonnet · 高
职责  独立会话审查 diff 与证据，判定四类结果之一
性格  怀疑式，「全绿」不等于验证过；无证据即未通过
边界  不延续规划会话、不修改代码、不伪造通过
产出  accepted / implementation_fix / contract_revision / global_replan
                                          [编辑角色提示词]
```

**我的状态**：✗ 整页骨架占位。

---

## 九 · 记忆页

**「L2 项目记忆 · L3 跨项目记忆」**

| 元素 | 内容 |
|---|---|
| 范围选择器 | `acp-engine 12 条` / `acp-sidecar 5 条` / `duet-app 1 条` / **跨项目记忆 `~/.acpflows · 5`** / 「管理项目…」 |
| 状态筛选 | `全部 12` · `active 9` · **`候选 2`** · `已失效 3` |
| 条目 | `mem-203` `constraint` `active`<br>「集成测试使用临时 SQLite，不得读写用户真实数据库」 |
| 条目 | `mem-188` `experience` `active`<br>「codex 默认权限档为 agent，需 `session/set_mode` 收权」 |

★ **记忆有「类型」**（`constraint` / `experience`）与**「候选」态**——
对话里那行「记忆候选：取消需两段（先 cancel，再等 stopReason）· 审核」
就是产生候选的地方。

**我的状态**：✗ 整页骨架占位。

---

## 十 · Skill 页

**「L4 Skill 库 · 版本化发布与回滚」**

| 元素 | 内容 |
|---|---|
| 项目选择器 | `acp-engine 4 Skill · 12 记忆` / `acp-sidecar 2 Skill · 5 记忆` / `duet-app 0 Skill · 1 记忆` / **全局 Skill `~/.acpflows · 9`** |
| 条目 | `rust-test-first` `v2.1` `active` **`命中 46`** |
| 条目 | `acp-fake-runtime` `v1.4` `active` `命中 12` |
| 条目 | `git-worktree-guard` `v0.4` **`draft`**<br>「校验未通过：frontmatter 缺 `description`」 |

★ Skill **有版本号**（`v2.1`）、**有命中计数**、**有校验**（draft 态会说明为什么没通过）。
对话里的注入写的是 `skill:rust-test-first@2.1`——**带版本**。

**我的状态**：✗ 整页骨架占位。

---

## 统计

| | 项数 | 做了 |
|---|---|---|
| 窗口栏 | 7 | 4 |
| 左栏 | 7 | 5（两项打折） |
| 主区 | 15 | 2 |
| 右栏 | 6 | 0 |
| 弹层 | 3 | 0 |
| 角色与 Runtime 页 | 6 | 0 |
| 记忆页 | 4 | 0 |
| Skill 页 | 4 | 0 |
| **合计** | **52** | **11** |

★ 主区 15 项做了 2 项，右栏与弹层是 0——这就是「对话页只做了 1%」的实测依据。
